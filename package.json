{
  "name": "codex-usage-dashboard-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "build:demo": "node scripts/build-demo.mjs dist/pages",
    "capture:media": "node scripts/capture-media.mjs",
    "pretest": "npm run build:demo",
    "test": "playwright test tests/dashboard.spec.mjs tests/demo.spec.mjs",
    "pretest:demo": "npm run build:demo",
    "test:demo": "playwright test tests/demo.spec.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
